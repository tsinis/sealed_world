name: cli
description: Command-line tools for a sealed_world eco-system.
version: 0.0.1
publish_to: none

environment:
  sdk: ^3.0.0

dependencies:
  change_case: ^1.1.0
  path: ^1.8.3 # From Google
  select_annotation: ^0.2.0

dev_dependencies:
  build_runner: ^2.4.6 # From Google
  dart_code_metrics: ^5.7.6 # Flutter Favorite
  select: ^0.4.1
