name: currencies_dart

on:
  # push: # TODO: After publishing as open source.
  pull_request:
    paths:
      - "packages/currencies_dart/**"
      - ".github/workflows/currencies_dart.yaml"

jobs:
  verify-currencies-dart:
    uses: ./.github/workflows/dart_package_workflow.yaml
    with:
      package_name: currencies_dart