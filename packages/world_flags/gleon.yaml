required_version: ">=0.1.0"
screenshots:
  - include: "**/test/goldens/**/*.png"
    mode: pixel
    diff:
      threshold: 0.0
      anti_alias: true
      min_similarity: 1
    masks: []
exclude:
  - build/**
