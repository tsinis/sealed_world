version: 2.2.0
name: world_flags
description: Country flags made with Flutter - every flag is a Widget, without any assets.
maintainer: Roman Cinis
repository: https://github.com/tsinis/sealed_world/tree/main/packages/world_flags
issue_tracker: https://github.com/tsinis/sealed_world/issues
topics:
  - flags
  - flag
  - country-flags
  - country-flag
  - coat-of-arms

screenshots:
  - description: "Example of usage"
    path: doc/example.webp

resolution: workspace

environment:
  sdk: ^3.7.0

dependencies:
  flutter:
    sdk: flutter
  sealed_countries: ^2.1.0

dev_dependencies:
  _sealed_world_tests:
    path: ../_sealed_world_tests
  arabic_font: ^0.0.9
  dart_code_metrics_presets: any # Constrained in the CLI tools package
  flutter_test: # From Google
    sdk: flutter
  golden_toolkit: ^0.15.0 # From eBay
  lints: any # From Google. Constrained in the CLI tools package
