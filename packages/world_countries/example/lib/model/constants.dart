sealed class Constants {
  static const slash = "/";
  static const code = "code";
  static const settings = "settings";

  // ignore: avoid-explicit-type-declaration, vs specify_nonobvious_property_types
  static const String settingsSuffix = slash + settings;
}
