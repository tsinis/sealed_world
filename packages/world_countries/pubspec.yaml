name: world_countries
description: Sealed world data in form of Flutter widgets (country, phone, currency pickers, etc.).
version: 0.0.1
homepage: https://github.com/tsinis/sealed_world/packages/world_countries

publish_to: "none" # TODO! Remove this line if you wish to publish to pub.dev

environment:
  sdk: ">=3.0.0-134 <4.0.0"
  flutter: ">=3.7.0-24"

dependencies:
  flutter:
    sdk: flutter
  sealed_countries:
    path: ../sealed_countries

dev_dependencies:
  build_runner: ^2.4.0 # From Google
  dart_code_metrics: ^5.5.1 # Flutter Favorite
  flutter_test: # From Google
    sdk: flutter
  lints: ^2.0.1 # From Google